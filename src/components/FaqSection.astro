---
import { faq } from "../constants/content";
---
<section class="space-y-md">
  <h3 class="font-display text-subheading tracking-wide">{faq.title}</h3>
  <div class="space-y-sm">
    {faq.items.map((item) => (
      <details class="section-frame p-md">
        <summary class="flex items-center justify-between gap-sm text-body">
          <span>{item.question}</span>
          <span class="details-marker text-text-tertiary">›</span>
        </summary>
        <p class="mt-sm text-body text-text-secondary">{item.answer}</p>
      </details>
    ))}
  </div>
</section>
