---
import PageSection from "./PageSection.astro";
import { sections } from "../constants/content";

const { shelf } = sections;
---
<PageSection
  id={shelf.id}
  page={shelf.page}
  title={shelf.title}
  subtitle={shelf.subtitle}
  description={shelf.description}
  descriptionClass="text-body text-text-secondary"
>
  <div class="grid gap-lg lg:grid-cols-[1.1fr_0.9fr] lg:items-start">
    <div class="grid gap-md">
      <div class="grid gap-md">
        <div class="grid gap-md sm:grid-cols-2 items-start">
          <div class="space-y-sm w-full max-w-[260px] mx-auto">
            <img
              class="block w-full h-auto rounded-lg bg-transparent"
              src={shelf.shelfImages.book1.src}
              alt={shelf.shelfImages.book1.alt}
              loading="lazy"
            />
            <p class="text-caption text-text-tertiary">カテゴリごとの図鑑が並びます。</p>
          </div>
          <div class="space-y-sm w-full max-w-[260px] mx-auto">
            <img
              class="block w-full h-auto rounded-lg bg-transparent"
              src={shelf.shelfImages.book2.src}
              alt={shelf.shelfImages.book2.alt}
              loading="lazy"
            />
            <p class="text-caption text-text-tertiary">図鑑には料理カードが並びます。</p>
          </div>
        </div>
      </div>
    </div>
    <div class="rounded-xl bg-background-secondary p-xl shadow-sm space-y-lg">
      <h3 class="font-display text-heading tracking-wide">{shelf.flowTitle}</h3>
      <div class="space-y-sm text-body text-text-secondary">
        {shelf.flowLines.map((line) => (
          <p>{line}</p>
        ))}
      </div>
      <div class="border-t border-black/10 pt-md space-y-sm">
        <p class="text-caption text-text-tertiary tracking-wide">記録を重ねるほど、カードの表情が変化します。</p>
        <div class="grid grid-cols-4 gap-sm w-full">
          {shelf.rankImages.map((image) => (
            <div class="space-y-xs">
              <div class="rounded-lg bg-background-primary/70 p-xs">
                <img class="block w-full h-auto rounded-md" src={image.src} alt={image.alt} loading="lazy" />
              </div>
              <p class="text-caption text-text-tertiary text-center">{image.label}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</PageSection>
